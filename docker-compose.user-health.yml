services:
  web-user:
    healthcheck:
      test: ["CMD-SHELL","node -e \"fetch('http://localhost/healthz').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))\""]
      interval: 10s
      timeout: 5s
      retries: 5
